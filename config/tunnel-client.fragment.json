{
  "_note": "MERGE these keys into your node/LapEE config; do not use as a standalone file. See config/README.md. Set peer to your broker URL.",
  "on": {
    "start": [
      {
        "device": "tunnel@1.0",
        "method": "POST",
        "path": "connect",
        "peer": "https://tunnel.example.com",
        "workers": 3,
        "hook": {
          "result": "ignore"
        }
      }
    ]
  }
}
